# Copy to .env.local for local development. Never commit the real value.

# Shared operator passphrase. Gates /operator (POST /api/auth) and, in Phase 3,
# every privileged backup/restore request. See docs/race-readiness-design.md
# "Auth model". In production this is set as a Vercel project env var.
PUBLISH_SECRET=
